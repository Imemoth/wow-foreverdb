local _, FDB = ...

function FDB:InitializeTooltip()
    -- Tooltip enrichment is intentionally deferred until the beta collector
    -- proves that NPC identification and observation counting are reliable.
    -- Keeping this module present makes the v0.1 file contract stable.
end
